
<%= @post.headline %>
<%= @post.topic.name unless @post.topic == nil  %> 
<% if @post.author %>by <%= @post.author %><%end%>
<%= truncate(@post.description, :length => 100, :omission =>"...", :separator => ' ') %> 

<%=  @post.url %>

View your entire Jurnalo feed here:
<%= root_url %>

View or unfollow all messages from <%= @post.user.name %> here:
<%= user_url(@post.user) %> 

<% if @post.utopic %>
View or block <%= @post.utopic.topic.name %> posts from <%= @post.user.name %> here:
<%= url_for(:controller => "users", :action => "show", :id => @post.user.id,  :topic => @post.utopic.topic.id, :only_path => false) %>
<%end%>
