
<div id="subscribe_form">

  	<% if current_user && current_user.subscribed?(@author) %>

		    <%= render 'authors/unsubscribe' %>
		
	<% else %>
		    <%= render 'authors/subscribe' %>
	<% end %>
	</div>

