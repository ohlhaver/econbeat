<% content_for :title, "Most Followed Economists - " %>
<div class ="upper_section">
<h3>Jurnalo's Most Followed Economists</h3><br><br>

<% @authors.each do |a| %>
	<% a=Author.find(a) %>
	<%= link_to a.name, author_path(a) %>,
	<%end%>

</div>