<% content_for :title, "Most Followed columnists - " %>
<div class ="upper_section">
<h3>Jurnalo's Most Followed columnists</h3><br><br>

<% @authors.each do |a| %>
	<% a=Author.find(a) %>
	<%= link_to a.name, author_path(a) %>,
	<%end%>

</div>